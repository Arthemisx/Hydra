# Copie este arquivo para .env e ajuste os valores antes de subir o docker-compose.
# cp .env.example .env

# --- Banco de dados Postgres ---
POSTGRES_USER=hydra
POSTGRES_PASSWORD=troque-esta-senha
POSTGRES_DB=hydra

# --- Backend ---
# OBRIGATORIO: segredo usado para assinar os tokens JWT.
# Gere um valor forte, por exemplo: openssl rand -hex 32
JWT_SECRET=troque-por-um-segredo-aleatorio-forte

# Popular o banco com usuarios demo no primeiro start (1 = sim, 0 = nao).
SEED_ON_START=1

# Numero de workers do gunicorn.
GUNICORN_WORKERS=3

# --- Integracoes opcionais (clima / IA) ---
# Sem chave o app funciona com respostas padrao (fallback).
OPENWEATHER_API_KEY=
OPENROUTER_API_KEY=
OPENROUTER_MODEL=meta-llama/llama-3-8b-instruct

# --- Frontend (build web) ---
# Deixe VAZIO para o app web usar o proxy /api do proprio nginx (recomendado).
EXPO_PUBLIC_API_BASE_URL=

# --- HTTPS (Caddy) ---
# Hostname publico do site. Para IP sem dominio, use o sslip.io: <IP>.sslip.io
# Ex.: 3.17.204.77.sslip.io  (necessario para a geolocalizacao funcionar no navegador)
SITE_HOSTNAME=3.17.204.77.sslip.io
